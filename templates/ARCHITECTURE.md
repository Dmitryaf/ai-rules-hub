# Архитектура `<название>`

## Статус документа

`<Какая часть описывает факт, а какая — целевое направление.>`

## Цели и ограничения

- `<...>`

## Фактическая архитектура

```text
<текущие компоненты и поток данных>
```

## Источники истины

| Категория | Владелец | Валидация | Копия/cache |
| --- | --- | --- | --- |
| `<...>` | `<...>` | `<граница>` | `<...>` |

## Направление зависимостей

```text
<допустимое направление между слоями или модулями>
```

- Разрешённые точки входа: `<...>`.
- Запрещённые связи: `<...>`.

## Механическая проверка границ

| Ограничение | Проверка | Где запускается |
| --- | --- | --- |
| `<направление импорта / схема / именование / другое>` | `<линтер, анализатор или структурный тест / не установлена>` | `<локально, CI / не установлено>` |

Не добавляй проверку только ради заполнения таблицы. Отсутствующий механизм фиксируется как разрыв и создаётся отдельной задачей.

## Целевое направление

- `<Будущая граница и условие, при котором она понадобится.>`

## Принятые решения и компромиссы

1. `<Решение, причина, цена.>`

## Неизвестное и условия пересмотра

- `<Что ещё не решено.>`
- `<Какой второй сценарий или риск заставит пересмотреть архитектуру.>`
