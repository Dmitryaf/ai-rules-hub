# Правила проекта `<название>`

Этот файл содержит только специфику данного проекта. Общие правила разработки подключаются через `RULESET.md` в этой же директории и не дублируются здесь. Пути к файлам проекта указываются относительно `.ai-rules/PROJECT_RULES.md`, например `../docs/README.md` или `../src/`.

При первичном подключении фиксируй только фактически существующие контракты. Ссылайся лишь на реальные канонические источники; отсутствующий источник помечай `не установлен`, неизвестную проверку — `не установлена`. Не создавай документацию, tooling или CI для заполнения карты или Definition of Done и не устраняй разрывы в рамках подключения: запиши их в `RULESET.md`.

## Назначение

`<Что создаёт проект, для кого и какой результат даёт.>`

## Источники истины

| Вопрос или данные | Канонический источник | Допустимые копии |
| --- | --- | --- |
| `<категория>` | `<файл/сервис/владелец>` | `<cache/snapshot/нет>` |

## Неприкосновенные ограничения

- `<Что нельзя нарушить и почему.>`

## Рабочий процесс

- Основная единица работы: `<vertical slice / defect / другой результат>`.
- Проверки: `<точные read-only команды>`.
- Формат коммита: `<ссылка или формат>`.
- Commit/push/release: только после `<условие разрешения>`.

## Структура проекта

- `<../каталог или модуль>` — `<локальная ответственность>`.

## Документация и маршрутизация контекста

- Индекс документации: `<../docs/README.md или не требуется>`.
- Каноническое описание продукта: `<../PRODUCT.md или другой относительный путь>`.
- Каноническая архитектура: `<../ARCHITECTURE.md или другой относительный путь>`.
- Публичная документация: `<что имеет внешнюю аудиторию и практическую цель / не установлена>`.
- Локальная или закрытая документация: `<где хранится внутренняя документация / не установлена>`.
- Критерий публикации: `<какая внешняя польза и проверка оправдывают публикацию>`.

| Тип задачи | Читать дополнительно |
| --- | --- |
| `<продуктовая / архитектурная / исследовательская / другая>` | `<только относящиеся к задаче относительные пути>` |

Этот раздел содержит карту и ссылки, а не содержание всей проектной документации. Не требовать чтения всей папки `docs/` перед каждой задачей.

## Правила изменения кода

- `<ограничение или обязательная практика, специфичная для этого проекта>`.

## Совместимость и данные

- `<Поддерживаемые версии и путь миграции.>`
- `<Семантика unknown/null/empty/zero.>`

## Product/UI constraints

- `<Тон, accessibility, обязательные сценарии и запреты.>`

## Не сейчас

- `<Осознанно отложенные направления и условие разморозки.>`

## Definition of Done

- `<Пользовательский/эксплуатационный результат.>`
- `<Тесты и ручные проверки.>`
- `<Документация и внешние gates.>`
