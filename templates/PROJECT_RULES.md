# Правила проекта `<название>`

Этот файл содержит только специфику проекта. Общие правила подключаются через `RULESET.md` в этой же директории. Расширенный вариант структуры доступен в шаблоне `templates/PROJECT_RULES.full.md` хаба.

При первичном подключении описывай существующее состояние и ссылайся только на реально существующие канонические источники. Если источник отсутствует, укажи `не установлен`; если проверка неизвестна — `не установлена`. Не создавай документацию, tooling или CI ради заполнения шаблона и не устраняй найденные разрывы в рамках подключения: зафиксируй их в `RULESET.md`.

## Назначение

`<Что создаёт проект, для кого и какой результат даёт.>`

## Неприкосновенные ограничения

- `<Что нельзя нарушить и почему.>`

## Структура и документация

- `<Значимые каталоги, канонические документы и узкие маршруты чтения для разных задач.>`

## Граница документации

- Публичная документация: `<что имеет внешнюю аудиторию и практическую цель / не установлена>`.
- Локальная или закрытая документация: `<где хранится внутренняя документация / не установлена>`.
- Критерий публикации: `<какая внешняя польза и проверка оправдывают публикацию>`.

## Проверки

- `<Точные команды и обязательные ручные проверки.>`

## Специфичные правила

- `<Локальные правила кода, данных, UI, совместимости или рабочего процесса.>`

## Не сейчас

- `<Осознанно отложенные направления и условие возврата к ним.>`
